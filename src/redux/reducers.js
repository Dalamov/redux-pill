// import {} from"./types";
// import {} from "./state";

const AppReducer = (state = null, action) => {
  switch (action.type) {
    case 'ACTION':
      return state
    default:
      return state
  }
}
export default AppReducer;